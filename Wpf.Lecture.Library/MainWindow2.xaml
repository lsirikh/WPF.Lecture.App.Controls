<Window x:Class="Wpf.Lecture.Library.MainWindow2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf.Lecture.Library"
		xmlns:views="clr-namespace:Wpf.Lecture.Library.UI.Views"
        mc:Ignorable="d"
        Title="MainWindow2" Height="450" Width="800">
	<Grid>
		<!--<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<ListBox 
			Grid.Row="0"
			ItemsSource="{Binding Items}"
			DisplayMemberPath="Name"
			SelectedItem="{Binding CurrentItem, Mode=TwoWay}">

		</ListBox>

		<TextBox 
			Grid.Row="1"
			Text="{Binding CurrentItem.Name, Mode=TwoWay}"/>
		-->
		<views:MainContent/>
	</Grid>
</Window>
